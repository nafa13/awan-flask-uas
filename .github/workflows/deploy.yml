name: Deploy to EC2

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
      
    - name: Placeholder for Testing
      run: |
        echo "Running Tests..."
        # python -m pytest
        
    - name: Placeholder for Deploy
      run: |
        echo "Deploying to AWS EC2 via SSH..."
        # Here we would use appleboy/ssh-action or similar to ssh into EC2
        # and run docker-compose pull && docker-compose up -d
